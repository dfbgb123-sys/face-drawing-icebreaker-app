module.exports = {
  PORT: process.env.PORT || 3000,
  DEFAULT_ROUND_MS: 3 * 60 * 1000,
  INTERMISSION_MS: 8 * 1000,
  SUBMIT_GRACE_MS: 5 * 1000,
  MAX_PNG_BYTES: 8 * 1024 * 1024,
  CANVAS_WIDTH: 800,
  CANVAS_HEIGHT: 600,
  MIN_PARTICIPANTS: 2,
  BATON_MAX_PARTICIPANTS: 15,
  RESULTS_SUSPENSE_MS: 3000
};
